apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-server

spec:
  template:
    spec:
      containers:
        - name: order-server
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"